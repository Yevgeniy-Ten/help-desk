stages:
  - deploy

before_script:
  - mysql --version
  - docker info
  - docker-compose -v

build_image:
  stage: deploy
  script:
    - docker-compose build
    - docker-compose up
  only:
    - master
    - merge_requests
